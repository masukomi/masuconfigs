#!/bin/sh

if [ ! -f .gitignore ]; then 
	echo ".vimrc" > .gitignore
	echo ".vscode" > .gitignore
	echo "tags" >> .gitignore

else
	>&2 echo "Leaving .gitignore in place"
  echo "Leaving existing .gitignore in place"
fi
