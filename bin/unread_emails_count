#!/bin/sh

# found here
# https://www.commandlinefu.com/commands/view/3380/check-your-unread-gmail-from-the-command-line
# basically just extracting some info from the atom feed of your email


email_passy=$(security find-generic-password -g -j "neomutt-masukomi@gmail.com" 2>&1 | grep password: | sed -e 's/password: //' -e 's/"//g'); 

curl -u masukomi@gmail.com:$email_passy --silent "https://mail.google.com/mail/feed/atom" | sed -e "s/.*<fullcount>//g" -e "s/<\/fullcount>.*//"
