# TooLoo documentation file.
# Details: https://github.com/masukomi/TooLoo
name="yaml_get_vname"
short_description="bash function to extract a variable from a yaml file"

description="""
given a path to a yaml file, and a couple arguments it can extract a
single variable.

If you need to extract multiple variables check out the parse_yaml function.
"""

#usage_command=""

fallback_usage="""
Usage: source <path/to/yaml_get_vname.bash>;
       yaml_get_vname <file_path> [<level:0>] [<prefix>]
Summary: Gets list of names of parent variables of given <level> in YAML-file.
Help:
  Params:
    <file_path> Path to YAML-file.
    [<level:|0|1|2...N>] Level of variable. Starts from 0.
    [<prefix>] Prefix which will be added to variable names.
"""

# is this an executable, function, or ???
type="executable"
# what language is it written in?
language="bash"
# where you can find this specific file in source control
source_url="https://github.com/masukomi/masuconfigs/blob/master/bin/lib/yaml_get_vname.bash"
# where you can find the repo that contains this file
source_repo_url="https://github.com/masukomi/masuconfigs/"

tags=[ "bash", "yaml"  ]
